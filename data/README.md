# Local data

This directory is intentionally ignored except for this file. Runtime SQLite
databases, exports, the private user profile, scraped evidence, and OAuth state
belong here and must never be committed.

